## Stack Template Configuration

# Human-friendly name to namespace containers
# Used as prefix in docker-compose container_name
STACK_NAME=tamasuma

# Public HTTP port exposed by Caddy
WEB_HTTP_PORT=8080

# Application repository (Kaido starterkit-based)
# Provide either full URL or "owner/repo". HTTPS by default; set APP_REPO_SSH=true for SSH.
APP_REPO=juniyasyos/tamasuma-backend
APP_REPO_BRANCH=development
APP_REPO_SSH=false

# Directory name under ./site where the repo will live
APP_DIR=tamasuma-backend

# Optional: override compose service names used by run.sh (normally no change needed)
# SERVICE_APP=app
# SERVICE_WEB=web
# SERVICE_DB=db
# SERVICE_NODE=node

# Database port mapping (host side). Container listens on 5432.
# DB_PORT=5432

